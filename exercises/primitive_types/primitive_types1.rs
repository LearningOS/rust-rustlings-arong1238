// primitive_types1.rs
// Fill in the rest of the line that has code missing!
// No hints, there's no tricks, just get used to typing these :)



fn main() {
    // Booleans (`bool`)

    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    let is_evening: bool = true;// 像示例一样完成这行！或者将其设为 false ！
    if is_evening {
        println!("Good evening!");
    }
}

